# Build artifacts
.zig-cache
zig-out
*.so
*.ld
*.o
*.bc

# Solana
.program-id
program-keypair*
test-ledger

# Dependencies (fetch with build.zig.zon)
vendor

# Toolchains (download separately)
solana-zig
roc-source
roc_nightly-*

# Archives (not needed after extraction)
*.tar.bz2
*.tar.xz
*.tar.gz

# Old/removed directories
solana-platform-*
platform-tools*
solana-rust
solana-llvm
solana-zig-bootstrap
llvm-*
zig-x86_64-linux-*

# Node.js
node_modules

# IDE
.vscode
.idea
